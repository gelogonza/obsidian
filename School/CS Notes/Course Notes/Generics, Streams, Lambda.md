---
Course: C212 Java
Date: 2025-02-24
---
# Testing ‘take’

```Java
take([3, 4, 12, 42, -6], 2) //output [3, 4]
take([], 10) => []
take([42, -10, 81], 0) => []
take([ 42, 81], 3) => []
```

```Java
static List<Integer> take(List<Integer> ls, int n) {
			if (n > ls.size()) {
					return new ArrayList();
					} else {
							List<Integer>ls = new ArrayList();
							for (int z = 0; cn; i++) {
							ls.add(ls.get(i));
							}
							return ls2;
							}
						}
```